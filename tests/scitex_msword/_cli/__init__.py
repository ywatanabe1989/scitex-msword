# Test mirror dir for src/scitex_msword/_cli/.
# Tests for individual CLI subcommands live as siblings; the package-level
# entry-point smoke test lives in ../test_cli.py (covers cli.main() + the
# wiring that brings each _cli/* register() into the root group).
